/* 
1. Create a variable.js file and declare variables and assign string, boolean, undefined and null data types, Display all the value with there type.
*/

let full_name = 'akshita'
console.log(`datatype of ${full_name} is: ${typeof(full_name)} `);

let is_valid = true
console.log(`datatype of ${is_valid} is: ${typeof(is_valid)} `);

let price
console.log(`datatype of ${price} is: ${typeof(price)} `);

let temp = null
console.log(`datatype of ${temp} is: ${typeof(temp)} `);
